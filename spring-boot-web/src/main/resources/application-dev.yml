spring:
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/spring?useSSL=false
      username: root 
      password: 123456
      initial-size: 5
      max-wait: 60000
      min-idle: 5
      max-active: 10

mybatis:
  type-aliases-package: com.entity
      
